var name = 'Eduardo', lastName = 'Zamarron', age = 24

var upperCaseName = name.toUpperCase();
var lowerCaseLastName = lastName.toLowerCase()
var firstLetterName = name.charAt(0)
var nameLength = name.length
var fullName = name + ' ' + lastName.toUpperCase();
var fullName2 = `${name} ${lastName.toUpperCase()}`
var str = name.substr(0,3)
var lastLetterName = name.charAt(name.length-1)
console.log(firstLetterName)
console.log(upperCaseName + ' ' + lowerCaseLastName)
console.log(nameLength)
console.log(fullName)
console.log(fullName2)
console.log(lastLetterName)

